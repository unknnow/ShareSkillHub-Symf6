<nav class="navbar navbar-expand-lg bg-white border-bottom border-gray">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}"><img src="{{ asset('build/images/logo.png') }}" alt="Logo" width="36" height="30"></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item mx-2 {% if app.request.attributes.get('_route') == 'app_home' %}active{% endif %}">
                    <a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
                </li>

                <li class="nav-item mx-2 {% if app.request.attributes.get('_route') == 'app_user_index' %}active{% endif %}">
                     <a class="nav-link" href="{{ path('app_user_index') }}">Utilisateurs</a>
                </li>

                <li class="nav-item mx-2 {% if app.request.attributes.get('_route') == 'app_session_index' %}active{% endif %}">
                     <a class="nav-link" href="{{ path('app_session_index') }}">Sessions</a>
                </li>

                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item mx-2">
                        {# <a class="nav-link" href="{{ path('admin') }}">Admin</a> #}
                    </li>
                {% endif %}
            </ul>

            <a class="nav-link dropdown-toggle me-3" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                {{ app.user.displayName }}
            </a>
            <ul class="dropdown-menu dropdown-menu-end me-5" aria-labelledby="navbarDropdown">
                <li>
                    {# <a class="dropdown-item d-flex justify-content-start align-items-center" href="{{ path('app_user_profil_show', {id: app.user.id}) }}">
                        <span class="material-icons-outlined me-2">info</span>
                        Mon profil
                    </a> #}
                </li>
                <li>
                    {# <a class="dropdown-item d-flex justify-content-start align-items-center" href="{{ path('app_user_competences', {id: app.user.id}) }}">
                        <span class="material-icons-outlined me-2">military_tech</span>
                        Mes compétences
                    </a> #}
                </li>
                <li>
                    {# <a class="dropdown-item d-flex justify-content-start align-items-center" href="{{ path('app_user_recommandations', {id: app.user.id}) }}">
                        <span class="material-icons-outlined me-2">star_rate</span>
                        Mes recommandations
                    </a> #}
                </li>
                <li>
                    {# <a class="dropdown-item d-flex justify-content-start align-items-center" href="{{ path('app_user_sessions', {id: app.user.id}) }}">
                        <span class="material-icons-outlined me-2">view_timeline</span>
                        Mes sessions
                    </a> #}
                </li>
                <li>
                    {# <a class="dropdown-item d-flex justify-content-start align-items-center" href="{{ path('app_user_security_show', {id: app.user.id}) }}">
                        <span class="material-icons-outlined me-2">lock</span>
                        Sécurité
                    </a> #}
                </li>
            </ul>

            <form class="d-flex">
                <a class="btn btn-sm btn-primary" href="{{ path('app_logout') }}">Déconnexion</a>
            </form>
        </div>
    </div>
</nav>
